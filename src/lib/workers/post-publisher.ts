// Worker wurde durch Vercel Cron ersetzt.
// Die Publish-Logik befindet sich jetzt in /api/cron/publish/route.ts

export function startWorker() {
  console.log("Cron-basiertes Publishing aktiv – kein Worker nötig");
}
